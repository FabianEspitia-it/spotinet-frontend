# URL base del backend (solo servidor: auth, dashboard, BFF `/api/upstream`).
# Ejemplo: https://api.tudominio.com
BACKEND_API_URL=http://localhost:8000

# Misma URL, expuesta al navegador para las páginas de streaming públicas
# (`src/lib/streaming-codes-client.ts`). Debe coincidir con BACKEND_API_URL.
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:8000

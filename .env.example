# URL base del backend (solo servidor; no uses NEXT_PUBLIC_*).
# Los segmentos por proveedor (`/netflix`, `/disney`, `/prime`, `/hbo`)
# se construyen en `src/lib/actions/streaming.ts`.
# Ejemplo: https://api.tudominio.com
BACKEND_API_URL=http://localhost:8000
